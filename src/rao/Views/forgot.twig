{% extends 'templates/base-login.twig' %}
{% block title %}Recuperar contraseña{% endblock %}
{% block css %}{% endblock %}
{% block contenido %}
    <form class="form-signin" id="forgotForm" role="form" method="post">
        <div class="alert alert-info">
        	<strong>Atención!</strong>
            Te enviaremos un correo electrónico con instrucciones para poder cambiar tu contraseña. Si no lo encuentras, verifica el correo no deseado.
        </div>
        <div class="form-group{% if errors.user %} has-error {% endif %}">
            <input type="email" name="inputEmail" id="inputEmail" class="form-control" placeholder="Correo electronico"
                   value="{{ old.inputEmail }}" required autofocus>
            <span class="help-block">{{ errors.inputEmail | first }}</span>
        </div>
        {{ csrf.field | raw }}
        <button class="btn btn-lg btn-danger btn-block" id="btnIngresar" type="submit">Enviar correo</button>
        <div class="bottom text-center">
            ¿Si la recuerdas? <a href="{{ path_for('auth.login') }}"><b>¡Inicia sesión!</b></a>
        </div>
        <div class="bottom text-center">
            ¿No tienes cuenta? <a href="{{ path_for('auth.signup') }}"><b>Regístrate</b></a>
        </div>
    </form>
{% endblock %}
{% block js %}

{% endblock %}